   # Composerが生成する依存ライブラリ（ファイル数が多い、composer.lockがあれば再現ができるためgit管理不要）
   src/vendor
   # npm版の依存ライブラリ（ファイル数が多い、package-lock.jsonがあれば再現ができるためgit管理不要）
   src/node_modules
   # テンプレートからコピーした.envは各ユーザー専用であるため、git管理しない
   src/.env